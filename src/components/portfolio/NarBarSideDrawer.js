import React, { useState } from "react";
import { Link } from "react-router-dom";

const NarBarSideDrawer = ({ navLinks }) => {
  return <div>Nav bar side drawer</div>;
};

export default NarBarSideDrawer;
